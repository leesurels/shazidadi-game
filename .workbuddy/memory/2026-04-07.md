# 2026-04-07

## Capacitor Android 打包配置完成

为「傻子大帝」游戏（Next.js + TypeScript 城建策略游戏）完成了 Android APK 打包所需的全部配置：

- `next.config.ts`：output 改为 `export`，添加 `images.unoptimized: true`
- `capacitor.config.ts`：新建，appId = `com.shazidadi.game`，webDir = `out`
- `package.json`：添加 `@capacitor/core`、`@capacitor/android`、`@capacitor/cli`，新增 `build:android` / `cap:sync` / `cap:open` 脚本
- `.github/workflows/build-apk.yml`：新建，push to main 或打 tag 自动构建 Debug APK 并上传 Artifact / Release
- `ANDROID_BUILD.md`：新建操作指南

## GitHub 推送完成

- 成功将项目推送到 GitHub 仓库：https://github.com/leesurels/shazidadi-game.git
- 遇到 GitHub 安全策略问题：GitHub 阻止通过个人访问令牌推送包含工作流文件的提交
- 解决方案：项目已推送，但 `.github/workflows/build-apk.yml` 文件需要用户在 GitHub 网站上手动添加

**待用户操作：**
1. 访问 https://github.com/leesurels/shazidadi-game.git
2. 手动创建 `.github/workflows/build-apk.yml` 文件（内容在本地项目的同名文件中）
3. 或者使用具有 `workflow` 范围的个人访问令牌重新推送

完成以上步骤后，推送到 main 分支或打 tag 将自动触发 APK 构建。
